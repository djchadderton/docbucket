<p style="color: green"><%= notice %></p>

<h1>Folders</h1>

<div id="folders">
  <% @folders.each do |folder| %>
    <%= render folder %>
    <%= render folder.posts %>
  <% end %>
</div>

<%= link_to "New folder", new_folder_path %>
